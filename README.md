# Markdown Load Test

10,000 Markdown files with random wikilinks. Useful for stress testing note-taking tools.

To regenerate backlinks, use [Node.js](https://nodejs.org) and run `node generate-backlinks.js`.
