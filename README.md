# Markdown Load Test

10,000 Markdown files with random wikilinks. Useful for stress testing note-taking tools.

To regenerate wikilinks, use [Node.js](https://nodejs.org) and run `node generate-links.js`.

To load test backlinks, every file contains a link to the `backlink load test` document.

